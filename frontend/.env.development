# frontend/.env.development
#
# In development, VITE_API_URL is intentionally empty.
# All /api/* requests are proxied to http://127.0.0.1:8000 by vite.config.js.
# This means the local Django server (hms.settings.dev) is used automatically.

VITE_API_URL=
